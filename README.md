# Client-Alok-Shende

This orgenization is created to work on apps for Client Alok

This web application is designed for a single user (personalized use).
The application will run locally on the user’s machine.
Both the backend and the database will operate on the local environment, while the frontend will be hosted on Netlify.
